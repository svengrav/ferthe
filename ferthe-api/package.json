{
  "name": "ferthe-api",
  "version": "1.0.0",
  "description": "Ferthe API server",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:production": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --sourcemap",
    "dev": "tsx watch --inspect=9229 src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.3.3",
    "ferthe-workspace": "file:.."
  }
}
